# fl-based-threat-detection
This project implements a privacy-preserving threat detection system using Federated Learning, a decentralized machine learning approach that enables multiple clients to collaboratively train a shared model without exchanging raw data.
